# Public frontend configuration. Values are baked into the browser bundle at build time.
# Do not put secrets in VITE_* variables.

# Production example: https://your-api.onrender.com
VITE_API_URL=http://localhost:8000

# Optional. Normally derived from VITE_API_URL (http -> ws, https -> wss).
# VITE_WS_URL=
